import React from 'react';

function IpToIdentity() {
  return (
    <div>
      <h3>IP-to-Identity Analysis</h3>
      <p>ตรวจสอบข้อมูลจาก IP เช่น ISP, city, country, และประวัติการเชื่อมต่อ</p>
      {/* TODO: Add IP input and results display */}
    </div>
  );
}

export default IpToIdentity;
